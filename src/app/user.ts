export interface User{

    firstName: string,
    lastName: string,
    age: number,
    address: string,
    phoneNum: number,
    email: string,
    
}
// a User type that the observable can be cast into